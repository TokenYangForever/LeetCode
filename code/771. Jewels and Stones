/**
 * @param {string} J
 * @param {string} S
 * @return {number}
 */
var numJewelsInStones = function(J, S) {
    return S.split('').reduce((pre, item) => {
        return J.includes(item) ? pre + 1 : pre
    }, 0);
};
